# Folder ini hanya berisi file PHP internal (require_once), tidak perlu diakses browser.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>

Options -Indexes
